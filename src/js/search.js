// const page = require('page'),
// dom = require('./elements.js');

// page('/search/:id', context => console.log('id', context.params.id));

// page();

// document.querySelector('#button').addEventListener('click', e => {
//   page('/search/123');
// });
