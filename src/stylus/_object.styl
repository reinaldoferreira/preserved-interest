.container
  center(65ex, vr-block(.5))

.list__item--inline
  display inline-block
  cf()

  + .list__item--inline
    margin-left .5rem

.f--left
  float left

.f--right
  float right

.s--sr
  clip rect(1px, 1px, 1px, 1px)
  position absolute
  height 1px
  width 1px
  overflow hidden
